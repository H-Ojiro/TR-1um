# TR-1um LVS v0.001 
# Original version was made by jun1okamura from TokaiRika's document 
# LICENSE: Apache License Version 2.0, January 2004,
#          http://www.apache.org/licenses/
# ----- ------ ----- ----- ------ ----- ----- ------ ----- 
# See: https://www.klayout.de/doc-qt5/manual/lvs_device_extractors.html
# Run mode {deep(default), flat}
if $run_mode == 'flat'
    puts("Setting running mode => flat")
    flat
else
    puts("Setting running mode => deep")
    deep          # Hierarchical extraction
end
# ----- ------ ----- ----- ------ ----- ----- ------ ----- 
# Define Top Cell
if $input 
    if $top_cell
        source($input, $top_cell)
    else
        source($input)
    end
end
# ----- ------ ----- ----- ------ ----- ----- ------ ----- 
# Extract Netlist Only
if $netlist_only
    NET_ONLY = true
else
    NET_ONLY = false
end
# ----- ------ ----- ----- ------ ----- ----- ------ ----- 
# Output report(lvsdb) file
if $report
    report_lvs($report)
else
    report_lvs    # LVS report window
end
# ----- ------ ----- ----- ------ ----- ----- ------ ----- 
# Extract Netlist Only
if $ignore_top_ports_mismatch
    IGNORE_TOP_PORTS_MISMATCH = true
else
    IGNORE_TOP_PORTS_MISMATCH = false
end

no_combine = defined?($no_combine) &&
               $no_combine.to_s.downcase == "true"
puts "no combine_devices (no_combine=true)" if no_combine

strict_stdcel_pins = defined?($strict_stdcel_pins) &&
                  $strict_stdcel_pins.to_s.downcase == "true"
puts "Unset equivalent_pins for STDCELL (strict_stdcel_pins=true)" if strict_stdcel_pins

#----
# %include ../../drc/IP62/00_Layers.drc
# %include ../../drc/IP62/02_Device.drc
# %include 00_Extract.lvs
# %include 01_Extract.lvs
# %include ../03_Combiner.lvs
# %include 02_Extract.lvs
# %include ../04_Custom.lvs
# %include ../05_Compare.lvs
### %include test.lvs
